# Profilo Docker: il database è già inizializzato dagli script in init-db/
# [3] data.sql: disattivato in Docker
# Non eseguire data.sql (risorse/data.sql): il DB è già popolato da init-db/,
# quindi evitiamo inserimenti duplicati.
spring.sql.init.mode=never
